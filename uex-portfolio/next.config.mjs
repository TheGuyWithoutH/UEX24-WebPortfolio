/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: "export",
  basePath: "/UEX24-WebPortfolio",
  images: {
    loader: "akamai",
    path: "",
  },
};

export default nextConfig;
